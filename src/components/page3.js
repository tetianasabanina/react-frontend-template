import React from 'react';
import { Grid, Typography } from '@material-ui/core';

const ThirdPage = () => (
  <Grid container>
    <Typography>You are on the 3rd component!</Typography>
  </Grid>
);

export default ThirdPage;
